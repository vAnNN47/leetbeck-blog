import './Loader.css';

export const Loader = () => {
    return (
        <div className="loader">
            <img className='spinner' src="/images/loader.webp" alt="Loading..." />
        </div>
    );
};